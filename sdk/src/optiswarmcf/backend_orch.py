#orchestrator for Sdk, manages the connection to the cf bridge and the swarm state
# it's main purpose is to provide a high level interface to the swarm, abstracting away the details of the connection to the cf bridge and the swarm state management
# it also provides a high level interface to the swarm, allowing to send commands to the swarm and to get the state of the swarm